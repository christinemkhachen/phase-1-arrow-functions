// 1) write a functions called divide
// // 2) divide divides 2000 by 100
// function divide() {
//   return(2000 / 100);
// }
//let name = (parameter or placeholder same thing) *or no placeholder at all* => *with arrow functions u always need the arrow after the = * {
//}
//this is the syntax
//let nameOfFunction = () => {
//return // the thing we want the function to return
//}

let divide = () => {
  return 2000 / 100;
};
let square = (a) => {
  return a * a;
};

let add = (a, b) => {
  return a + b;
};
